header.header
  nav.nav.nav--tours
    a.nav__el(href='/overview') All Stores
  .header__logo
    img(src='/img/logo-green.png' alt='Merch-Manage logo')
  nav.nav.nav--user
    if user
      button.nav__el.nav__el--logout Log out
      a.nav__el(href='/account') 
        img.nav__user-img(src='/img/logo-white.png' alt='user photo')
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up